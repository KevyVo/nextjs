name: Defang
on:
  push:
    branches:
      - main
  workflow_dispatch:
    inputs:
      action:
        description: Whether to deploy up or down
        required: true
        default: up
        type: choice
        options:
          - up
          - down
      stack:
        description: The stack to deploy up or down. (Leave blank for default)
        default: wip
jobs:
  defang:
    name: Defang ${{ github.event.inputs.action || 'up' }} ${{ github.event.inputs.stack || 'wip' }}
    runs-on: ubuntu-latest
    permissions:
      contents: read
      id-token: write
    concurrency:
      cancel-in-progress: false
      group: deploy-nextjs-${{ github.event.inputs.stack || 'wip' }}
    steps:
      - name: Checkout Repo
        uses: actions/checkout@v4
      - name: Defang ${{ github.event.inputs.action || 'up' }} ${{ github.event.inputs.stack || 'wip' }}
        uses: DefangLabs/defang-github-action@v1
        with:
          command: ${{ github.event.inputs.action || 'up' }}
          project: nextjs
          stack: ${{ github.event.inputs.stack || 'wip' }}
      - name: Deployment Summary
        uses: DefangLabs/defang-github-action@v1
        with:
          command: services
          project: nextjs
          stack: ${{ github.event.inputs.stack || 'wip' }}
    environment: defang-wip
